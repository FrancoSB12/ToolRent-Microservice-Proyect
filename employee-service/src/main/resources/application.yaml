spring:
  application:
    name: employee-service
  config:
    import: "optional:configserver:http://localhost:8081"
  datasource:
    url: jdbc:postgresql://localhost:5432/ToolRent-employee
    username: postgres
    password: Almeria12!
  jpa:
    hibernate:
      ddl-auto: create