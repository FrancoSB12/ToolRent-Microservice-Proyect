spring:
  application:
    name: client-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/ToolRent-client
    username: postgres
    password: Almeria12!
  jpa:
    hibernate:
      ddl-auto: create