spring:
  application:
    name: rent-service
  config:
    import: "optional:configserver:http://localhost:8081"
  datasource:
    url: jdbc:postgresql://localhost:5432/ToolRent-rent
    username: postgres
    password: Almeria12!
  jpa:
    hibernate:
      ddl-auto: update